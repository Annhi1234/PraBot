module.exports = {
  apps: [
    {
      name: 'prabot',
      script: 'index.js',
      // Якщо процес впаде (крашнеться) — pm2 підніме його знову
      autorestart: true,
      // Якщо памʼять перевищить ліміт (малоймовірно з поточним кодом,
      // але як страховка на слабкому телефоні) — pm2 перезапустить процес
      max_memory_restart: '150M',
      // Не спамити перезапусками, якщо щось падає постійно (наприклад,
      // невалідний токен) — почекати секунду між спробами
      restart_delay: 1000,
      max_restarts: 20,
      env: {
        NODE_ENV: 'production'
      }
    }
  ]
};
